# Tasks

## Current Task

Fix HTML5 validity regression in `replace_urls` (issue #29) — swap default
parser to HTML5 with `parser: html4|html5` opt-out, JRuby hard-error, no-op
short-circuit. Breaking change.

Checklists and component lists will be populated in the Plan phase.
